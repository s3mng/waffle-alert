spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:waffle_alert;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

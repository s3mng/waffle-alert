# 테스트 DB는 Testcontainers(MySQLTestContainerConfig)가 띄우고
# @ServiceConnection 으로 datasource 가 자동 주입된다. 여기엔 접속정보를 두지 않는다.
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
